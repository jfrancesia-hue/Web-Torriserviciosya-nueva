name: Deploy to Hostinger

on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: hostinger-production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Upload public site files
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy via FTP/SFTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.HOSTINGER_FTP_SERVER || secrets.FTP_SERVER }}
          username: ${{ secrets.HOSTINGER_FTP_USERNAME || secrets.FTP_USERNAME }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD || secrets.FTP_PASSWORD }}
          server-dir: ${{ secrets.HOSTINGER_FTP_TARGET_DIR || secrets.FTP_TARGET_DIR || '/public_html/' }}
          local-dir: ./
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/.pnpm-store/**
            **/.env
            **/.env.*
            webhook/.env
            webhook/*.txt
            webhook/*.json
            webhook/*.zip
            DEPLOY_INSTRUCTIONS.txt
